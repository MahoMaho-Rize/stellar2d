% Auto-generated by scripts/e3_top_outgoing_bc.py
% Do not edit manually.

% Alfvén invariants at the top boundary (y=L)
\begin{equation}\label{eq:E3_top_roles}
\tilde z^+ \text{ outgoing at } +v_A,\qquad \tilde z^- \text{ incoming at } -v_A
\end{equation}

% Top non-reflecting characteristic BC
\begin{equation}\label{eq:E3_BC}
\tilde z^-\bigr|_{\mathrm{top\;ghost}} = 0,\qquad \tilde z^+\bigr|_{\mathrm{top\;ghost}} = \tilde z^+\bigr|_{\mathrm{top\;int}}
\end{equation}

% Top ghost-fill closure — primitives
\begin{equation}\label{eq:E3_ghost_fill}
v_x\bigr|_{\mathrm{top\;ghost}} = \tfrac{1}{2}\bigl[v_x^{\mathrm{int}} - B_x^{\mathrm{int}}/\sqrt{\rho_0}\bigr],\qquad \frac{B_x\bigr|_{\mathrm{top\;ghost}}}{\sqrt{\rho_0}} = \tfrac{1}{2}\bigl[-v_x^{\mathrm{int}} + B_x^{\mathrm{int}}/\sqrt{\rho_0}\bigr]
\end{equation}

% Top reflection coefficient (linear)
\begin{equation}\label{eq:E3_reflection}
R_{\mathrm{top}} \equiv \tilde z^-\bigr|_{\mathrm{top\;ghost}}/\tilde z^+\bigr|_{\mathrm{top\;int}} = 0
\end{equation}

% Composite §E2 + §E3 linear steady state
\begin{equation}\label{eq:E3_composite_steady}
\tilde z^+(y) = -2\,v_x^{\mathrm{drv}},\qquad \tilde z^-(y) = 0\qquad\forall y\in[0,L_y]
\end{equation}

% WKB upgoing amplitude growth (Leroy80 / Cranmer07)
\begin{equation}\label{eq:E3_wkb_growth}
\frac{\mathrm{d}}{\mathrm{d}y}\bigl[A^2\,\rho\,v_A\bigr] = 0\quad\Longrightarrow\quad A \propto \rho^{-1/4}
\end{equation}
