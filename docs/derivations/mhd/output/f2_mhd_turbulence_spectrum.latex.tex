% Auto-generated by scripts/f2_mhd_turbulence_spectrum.py
% Do not edit manually.

% Kolmogorov-Obukhov (K41) spectrum
\begin{equation}\label{eq:F2_K41}
E_K(k) = C_K\,\epsilon^{2/3}\,k^{-5/3},\qquad C_K \approx 1.5\ \text{(classical)}
\end{equation}

% Iroshnikov-Kraichnan (IK65) MHD spectrum
\begin{equation}\label{eq:F2_IK}
E_{IK}(k) = C_{IK}\,(\epsilon\,v_A)^{1/2}\,k^{-3/2},\qquad C_{IK} \approx 2.1\ \text{(numerical)}
\end{equation}

% Dissipation wavenumber (Kolmogorov)
\begin{equation}\label{eq:F2_kdiss}
k_{\rm diss} = \bigl(\epsilon/\nu_{\rm eff}^3\bigr)^{1/4}
\end{equation}

% Inversion: ν_eff from measured k_diss
\begin{equation}\label{eq:F2_nu_from_kdiss}
\nu_{\rm eff} = \bigl(\epsilon/k_{\rm diss}^4\bigr)^{1/3}
\end{equation}

% Scheme-order consistency
\begin{equation}\label{eq:F2_scaling}
\nu_{\rm eff}(N) \propto \Delta x^2 \propto N^{-2}\ \Longrightarrow\ k_{\rm diss}(N) \propto N^{3/2}
\end{equation}

% A2 test-pass criterion
\begin{equation}\label{eq:F2_pass}
\text{(a) } E(k) \text{ shows inertial range with slope in } [-5/3, -3/2]\ \text{over 1 decade}\\\text{(b) } k_{\rm diss}(N=256) / k_{\rm diss}(N=128) \in [2.0, 3.5]\ \text{(expect } 2^{3/2} = 2.83\text{)}
\end{equation}
