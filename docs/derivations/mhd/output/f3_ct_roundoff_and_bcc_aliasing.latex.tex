% Auto-generated by scripts/f3_ct_roundoff_and_bcc_aliasing.py
% Do not edit manually.

% CT round-off accumulation bound (random walk)
\begin{equation}\label{eq:F3_roundoff}
\max_t |\nabla\!\cdot\!\mathbf{B}| \ \le\ 4\,\sqrt{n_{\mathrm{step}}}\,\varepsilon_{\mathrm{ULP}}\,|\mathbf{B}|_\infty / h
\end{equation}

% CT round-off accumulation bound (worst case, coherent sign)
\begin{equation}\label{eq:F3_roundoff_worst}
\max_t |\nabla\!\cdot\!\mathbf{B}| \ \le\ 4\,n_{\mathrm{step}}\,\varepsilon_{\mathrm{ULP}}\,|\mathbf{B}|_\infty / h
\end{equation}

% Midpoint B_cc reconstruction for smooth B
\begin{equation}\label{eq:F3_midpoint}
B_{x,\mathrm{cc}} \equiv \tfrac{1}{2}(B_{x,L} + B_{x,R}) = B_x(x_i) + \tfrac{h^2}{8} B_x'' + \mathcal{O}(h^4)
\end{equation}

% B_cc aliasing bound at field-loop kink (r = R)
\begin{equation}\label{eq:F3_aliasing}
\bigl|\,\mathrm{ME}_{\mathrm{cc}}(t) - \mathrm{ME}_{\mathrm{cc}}(0)\,\bigr|\ \le\ C_{\mathrm{alias}}\,A_0^2\,\pi R\,(h/R),\quad \text{oscillatory in }t
\end{equation}

% Physical interpretation
\begin{equation}\label{eq:F3_interp}
\text{CT conserves face-integrated flux exactly; the discrepancy lives in the diagnostic reconstruction }B_{\mathrm{cc}}\text{, not the solver.}
\end{equation}
