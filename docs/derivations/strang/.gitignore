# Analytic golden values (per Rule 5 of ../README.md).
# Regenerated by `bash run_all.sh`; consumed by tests/test_strang_*.cu.
output/d*.goldens.json
output/e*.goldens.json

# Pre-normalize backup from build_manuscript.sh (one build cycle).
manuscript.md.pre_norm
