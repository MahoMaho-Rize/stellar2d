% Auto-generated by scripts/a06_smooth_wave_families.py
% Do not edit manually.

% Riemann invariants (1-family, lambda = u - c)
\begin{equation}\label{eq:A6-RI-1}
J_1^{(1)} \;=\; u + \frac{2c}{\gamma - 1}, \qquad J_1^{(2)} \;=\; \frac{p}{\rho^{\gamma}}
\end{equation}

% Riemann invariants (3-family, lambda = u + c)
\begin{equation}\label{eq:A6-RI-3}
J_3^{(1)} \;=\; u - \frac{2c}{\gamma - 1}, \qquad J_3^{(2)} \;=\; \frac{p}{\rho^{\gamma}}
\end{equation}

% Riemann invariants (2-family, linearly degenerate)
\begin{equation}\label{eq:A6-RI-2}
\text{entropy wave: } u, p \text{ both constant through.}\quad \text{shear wave: } \rho, u, p \text{ all constant; only tangential } v \text{ jumps.}
\end{equation}

% Genuine nonlinearity (1- and 3-family)
\begin{equation}\label{eq:A6-gn}
\nabla_{\mathbf{W}}\lambda_1 \cdot R_1 \;=\; -\frac{(\gamma + 1)\,c}{2\rho} \;\neq\; 0, \qquad \nabla_{\mathbf{W}}\lambda_3 \cdot R_3 \;=\; +\frac{(\gamma + 1)\,c}{2\rho} \;\neq\; 0
\end{equation}

% Linear degeneracy (2-family)
\begin{equation}\label{eq:A6-ld}
\nabla_{\mathbf{W}}\lambda_2 \cdot R_{2a} \;=\; 0, \qquad \nabla_{\mathbf{W}}\lambda_2 \cdot R_{2b} \;=\; 0
\end{equation}

% Rankine-Hugoniot mass-, momentum-, and energy-jump conditions
\begin{equation}\label{eq:A6-RH}
\sigma\,[\rho] \;=\; [\rho u_n], \qquad \sigma\,[\rho u_n] \;=\; [\rho u_n^{2} + p], \qquad \sigma\,[E] \;=\; [(E + p)\,u_n]
\end{equation}

% Hugoniot locus for a 1-shock (Toro 4.38)
\begin{equation}\label{eq:A6-hugoniot-1shock}
\rho_{*L} \;=\; \rho_L\,\frac{p^{\star}/p_L + (\gamma-1)/(\gamma+1)}{(\gamma-1)/(\gamma+1)\,p^{\star}/p_L + 1}, \quad u^{\star} \;=\; u_L - f_L(p^{\star}), \quad \sigma \;=\; u_L - c_L\,\sqrt{\tfrac{\gamma+1}{2\gamma}\,\tfrac{p^{\star}}{p_L} + \tfrac{\gamma-1}{2\gamma}}
\end{equation}

% Contact discontinuity (2-family)
\begin{equation}\label{eq:A6-contact}
\text{Across a contact: } u_L = u_R = u^{\star}, \ p_L = p_R = p^{\star}; \ \rho\ \text{and}\ v_t\ \text{may jump.}
\end{equation}
