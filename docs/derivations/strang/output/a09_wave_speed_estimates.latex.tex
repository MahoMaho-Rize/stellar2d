% Auto-generated by scripts/a09_wave_speed_estimates.py
% Do not edit manually.

% Davis wave-speed estimates (used by kernel)
\begin{equation}\label{eq:A9-Davis}
S_L \;=\; \min(u_L - c_L,\ u_R - c_R), \qquad S_R \;=\; \max(u_L + c_L,\ u_R + c_R)
\end{equation}

% Einfeldt tighter bounds (used by HLLE/Roe-variant solvers)
\begin{equation}\label{eq:A9-Einfeldt}
S_L \;=\; \min\!\bigl(u_L - c_L,\ \tilde u - \tilde c\bigr), \qquad S_R \;=\; \max\!\bigl(u_R + c_R,\ \tilde u + \tilde c\bigr)
\end{equation}

% Roe-averaged primitive state
\begin{equation}\label{eq:A9-Roe-avg}
\tilde \rho \;=\; \sqrt{\rho_L\,\rho_R}, \qquad \tilde u \;=\; \frac{\sqrt{\rho_L}\,u_L + \sqrt{\rho_R}\,u_R}{\sqrt{\rho_L} + \sqrt{\rho_R}}, \\[4pt] \tilde v \;=\; \frac{\sqrt{\rho_L}\,v_L + \sqrt{\rho_R}\,v_R}{\sqrt{\rho_L} + \sqrt{\rho_R}}, \qquad \tilde h \;=\; \frac{\sqrt{\rho_L}\,h_L + \sqrt{\rho_R}\,h_R}{\sqrt{\rho_L} + \sqrt{\rho_R}}, \\[4pt] \tilde c^{\,2} \;=\; (\gamma - 1)\left(\tilde h - \tfrac{1}{2}(\tilde u^{2} + \tilde v^{2})\right)
\end{equation}

% Roe property
\begin{equation}\label{eq:A9-Roe-property}
A_{\mathrm{Roe}}(\mathbf{U}_L, \mathbf{U}_R)\,(\mathbf{U}_R - \mathbf{U}_L) \;=\; \mathbf{F}_x(\mathbf{U}_R) - \mathbf{F}_x(\mathbf{U}_L)
\end{equation}
