% Auto-generated by scripts/a10_slope_limiter_family.py
% Do not edit manually.

% Sweby's TVD region
\begin{equation}\label{eq:A10-Sweby-region}
0 \;\le\; \phi(r) \;\le\; \min(2r,\ 2)\quad (r \ge 0), \qquad \phi(r) = 0 \ \text{for } r < 0
\end{equation}

% Second-order accuracy (consistent slope at linear reconstruction)
\begin{equation}\label{eq:A10-second-order}
\phi(1) \;=\; 1
\end{equation}

% Limiter family, Sweby form
\begin{equation}\label{eq:A10-limiter-family}
\begin{aligned}\phi_{\mathrm{minmod}}(r) &\;=\; \max(0,\ \min(1,\ r)) \\[2pt]\phi_{\mathrm{VL}}(r) &\;=\; \frac{r + |r|}{1 + |r|} \\[2pt]\phi_{\mathrm{MC}}(r) &\;=\; \max\!\left(0,\ \min\!\left(2r,\ \tfrac{1+r}{2},\ 2\right)\right) \\[2pt]\phi_{\mathrm{superbee}}(r) &\;=\; \max\!\left(0,\ \min(2r, 1),\ \min(r, 2)\right) \\[2pt]\phi_{\mathrm{Ospre}}(r) &\;=\; \frac{3(r^{2} + r)}{2(r^{2} + r + 1)}\end{aligned}
\end{equation}

% Comparison at r = 3/2 (moderate gradient)
\begin{equation}\label{eq:A10-comparison}
\phi_{\mathrm{minmod}}(3/2) = 1 \;<\; \phi_{\mathrm{Ospre}}(3/2) = 45/38 \;<\; \phi_{\mathrm{VL}}(3/2) = 6/5 \;<\; \phi_{\mathrm{MC}}(3/2) = 5/4 \;<\; \phi_{\mathrm{superbee}}(3/2) = 3/2
\end{equation}

% Symmetry property (minmod, VL, MC, superbee, Ospre all satisfy)
\begin{equation}\label{eq:A10-symmetry}
\phi(1/r) \;=\; \phi(r) / r \qquad (r > 0)
\end{equation}

% Kernel's d_mc_limit two-argument form (same-sign branch)
\begin{equation}\label{eq:A10-kernel-form}
\mathrm{d\_mc\_limit}(a, b) \;=\; \mathrm{sign}(a)\,\min\!\left(\tfrac{|a+b|}{2},\ 2|a|,\ 2|b|\right), \quad \mathrm{sign}(a)\mathrm{sign}(b) > 0; \quad 0\ \text{otherwise}
\end{equation}
