% Auto-generated by scripts/a11_reconstruction_order.py
% Do not edit manually.

% Donor-cell reconstruction (1st order)
\begin{equation}\label{eq:A11-donor}
u_{j+1/2, L}^{\mathrm{donor}} \;=\; u_j; \qquad \varepsilon^{\mathrm{donor}} \;=\; -\tfrac{h}{2}\,u'(x_j) - \tfrac{h^{2}}{8}\,u''(x_j) + O(h^{3})
\end{equation}

% Unlimited MUSCL reconstruction (2nd order in full Godunov update)
\begin{equation}\label{eq:A11-MUSCL}
u_{j+1/2, L}^{\mathrm{MUSCL}} \;=\; u_j + \tfrac{h}{2}\,\sigma_j, \qquad \sigma_j = \frac{u_{j+1} - u_{j-1}}{2 h}; \\[4pt] \varepsilon^{\mathrm{MUSCL}} \;=\; -\tfrac{h^{2}}{8}\,u''(x_j) + \tfrac{h^{3}}{16}\,u'''(x_j) + O(h^{4})
\end{equation}

% Colella-Woodward PPM reconstruction (4th-order at the face)
\begin{equation}\label{eq:A11-PPM-CW}
u_{j+1/2}^{\mathrm{PPM\text{-}CW}} \;=\; \tfrac{7}{12}\bigl(u_j + u_{j+1}\bigr) - \tfrac{1}{12}\bigl(u_{j-1} + u_{j+2}\bigr); \qquad \varepsilon^{\mathrm{PPM}} \;=\; O(h^{4})
\end{equation}

% Colella-Sekora variant (extremum-preserving limiter)
\begin{equation}\label{eq:A11-PPM-CS}
\text{Same unlimited reconstruction as CW; limiter replaced by an extremum detector that preserves 3rd-order accuracy at smooth extrema.}
\end{equation}

% Reconstruction-order hierarchy (leading truncation)
\begin{equation}\label{eq:A11-hierarchy}
\text{donor-cell: } O(h) \;<\; \text{MUSCL (unlimited): } O(h^{2}) \;<\; \text{PPM: } O(h^{4})
\end{equation}
