% Auto-generated by scripts/b01_perturbation_storage.py
% Do not edit manually.

% Storage convention
\begin{equation}\label{eq:B1-store}
\mathbf{U}_{\text{store}} \;=\; \bigl(\delta\rho,\; m_x,\; m_y,\; \delta E\bigr)^{\mathsf T} \;=\; \bigl(\rho - \bar\rho(y),\;\rho u,\;\rho v,\;E_{\mathrm{tot}} - \bar p(y)/(\gamma-1)\bigr)^{\mathsf T}
\end{equation}

% Inverse (decode before arithmetic)
\begin{equation}\label{eq:B1-decode}
\rho = \delta\rho + \bar\rho,\qquad u = m_x/\rho,\qquad v = m_y/\rho,\qquad P = (\gamma-1)\,\bigl[\delta E + \bar p/(\gamma-1)\bigr] - \tfrac{1}{2}\rho(u^2+v^2)
\end{equation}

% Pressure-perturbation identity
\begin{equation}\label{eq:B1-dP}
\delta P \;\equiv\; P - \bar p \;=\; (\gamma-1)\bigl[\delta E - \tfrac{1}{2}\rho(u^2+v^2)\bigr]
\end{equation}

% Zero-perturbation invariant
\begin{equation}\label{eq:B1-zero-pert}
\delta\rho = m_x = m_y = \delta E = 0 \;\Longleftrightarrow\; (\rho, u, v, P) = (\bar\rho,\,0,\,0,\,\bar p)
\end{equation}

% Positive Jacobian (smooth bijection on rho>0, P>0)
\begin{equation}\label{eq:B1-jacobian}
\det\frac{\partial \mathbf{U}_{\text{store}}}{\partial \mathbf{W}} \;=\; \frac{\rho^{2}}{\gamma-1} \;>\; 0
\end{equation}
