% Auto-generated by scripts/b03_face_hse_reconstruction.py
% Do not edit manually.

% Face y-coordinate (both neighbours use the same)
\begin{equation}\label{eq:B3-face-y}
y_{\mathrm{face}} \;=\; y_{\mathrm{lo}} + j_{\mathrm{face}}\,\Delta y \qquad\text{(not } y_j \text{ or } y_{j+1} \text{ separately)}
\end{equation}

% Face reconstruction (both sides use same HSE background)
\begin{equation}\label{eq:B3-face-recon}
\begin{aligned}\rho_{L/R} \;&=\; \bar\rho(y_{\mathrm{face}}) \;+\; (\delta\rho_{j/j+1} \mp \tfrac{1}{2}\,s^\rho_{j/j+1}) \\P_{L/R}   \;&=\; \bar p(y_{\mathrm{face}}) \;+\; (\delta P_{j/j+1} \mp \tfrac{1}{2}\,s^P_{j/j+1}) \\u_{L/R}   \;&=\; u_{j/j+1} \mp \tfrac{1}{2}\,s^u_{j/j+1} \\v_{L/R}   \;&=\; v_{j/j+1} \mp \tfrac{1}{2}\,s^v_{j/j+1}\end{aligned}
\end{equation}

% Well-balancing (WB) necessary condition
\begin{equation}\label{eq:B3-WB}
\text{On pure HSE: } \delta\rho_j \equiv 0,\; \delta P_j \equiv 0,\; u_j \equiv 0,\; v_j \equiv 0,\; s \equiv 0 \;\Longrightarrow\; \mathbf{U}_L \;=\; \mathbf{U}_R \;=\; (\bar\rho,\,0,\,0,\,\bar p/(\gamma-1))^{\mathsf T}
\end{equation}

% HSE face flux
\begin{equation}\label{eq:B3-face-flux}
\mathbf{F}_y(\mathbf{U}_L) \;=\; \mathbf{F}_y(\mathbf{U}_R) \;=\; (0,\;0,\;\bar p(y_{\mathrm{face}}),\;0)^{\mathsf T}
\end{equation}

% Wrong (cell-centred) reconstruction: leading spurious flux jump
\begin{equation}\label{eq:B3-wrong}
\rho_L^{\mathrm{wrong}} - \rho_R^{\mathrm{wrong}} \;=\; -\,\Delta y\,\frac{d\bar\rho}{dy} \;\neq\; 0 \;\;\text{on pure HSE, which breaks WB at O(}\Delta y\text{)}
\end{equation}
