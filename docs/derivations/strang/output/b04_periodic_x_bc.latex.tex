% Auto-generated by scripts/b04_periodic_x_bc.py
% Do not edit manually.

% Periodic BC in x
\begin{equation}\label{eq:B4-periodic}
\mathbf{U}(x + L_x, y, t) \;=\; \mathbf{U}(x, y, t) \qquad \forall\,(x, y, t)
\end{equation}

% Ghost-cell copy (left side)
\begin{equation}\label{eq:B4-ghost-L}
\mathbf{U}_{\mathrm{ghost}}(i_{\mathrm{phys}} = -1 - g) \;=\; \mathbf{U}(i_{\mathrm{phys}} = n_x - 1 - g), \quad g \in \{0, 1, \ldots, n_g - 1\}
\end{equation}

% Ghost-cell copy (right side)
\begin{equation}\label{eq:B4-ghost-R}
\mathbf{U}_{\mathrm{ghost}}(i_{\mathrm{phys}} = n_x + g) \;=\; \mathbf{U}(i_{\mathrm{phys}} = g), \quad g \in \{0, 1, \ldots, n_g - 1\}
\end{equation}

% Domain periodicity identity
\begin{equation}\label{eq:B4-distance}
x_{\mathrm{src}} - x_{\mathrm{ghost}} \;=\; L_x \;=\; n_x\,\Delta x
\end{equation}

% Minimum ghost width (MUSCL-Hancock)
\begin{equation}\label{eq:B4-min-ng}
n_g \geq 2 \qquad\text{(one layer for slope, one for face-state refill)}
\end{equation}

% Flux commutativity
\begin{equation}\label{eq:B4-flux-commute}
\mathbf{F}_x(\mathbf{U}_{\mathrm{ghost}}) \;=\; \mathbf{F}_x(\mathbf{U}_{\mathrm{phys}}) \qquad \text{(pointwise function of }\mathbf{U}\text{)}
\end{equation}
