% Auto-generated by scripts/b06_outflow_y_bc.py
% Do not edit manually.

% Zero-gradient BC (top outflow)
\begin{equation}\label{eq:B6-zero-gradient}
\mathbf{U}_{\mathrm{ghost}}(j_g = n_g + n_y + g) \;=\; \mathbf{U}(j_g = n_g + n_y - 1),\quad g \in \{0, 1, \ldots, n_g - 1\}
\end{equation}

% Continuum interpretation (Neumann BC)
\begin{equation}\label{eq:B6-neumann}
\frac{\partial \mathbf{U}}{\partial y}\bigg|_{y=y_{\mathrm{top}}} \;=\; \mathbf{0}
\end{equation}

% Characteristic eigenvalues (y-flux Jacobian)
\begin{equation}\label{eq:B6-eigenvalues}
\mathrm{spec}(\mathcal{A}_y) \;=\; \{v - c,\; v,\; v,\; v + c\}
\end{equation}

% Subsonic outflow (0 < v < c): one incoming characteristic
\begin{equation}\label{eq:B6-subsonic}
v - c < 0 \;\text{ incoming};\quad v,\, v,\, v + c > 0 \;\text{ outgoing (3 modes)}
\end{equation}

% Supersonic outflow (v > c): all-outgoing, zero-gradient exact
\begin{equation}\label{eq:B6-supersonic}
v > c \;\Longrightarrow\; \text{all 4 eigenvalues} > 0
\end{equation}

% Incoming Riemann invariant (1D analogue)
\begin{equation}\label{eq:B6-R-minus}
R_{-} \;=\; u - \frac{2c}{\gamma-1}
\end{equation}

% Leading-order extrapolation error (subsonic outflow)
\begin{equation}\label{eq:B6-error}
R_{-}^{\mathrm{extrap}} \;-\; R_{-}^{\mathrm{true}} \;=\; -\,\tfrac{\Delta y}{2}\,\frac{dR_{-}}{dy} \;+\; O(\Delta y^{2})
\end{equation}
