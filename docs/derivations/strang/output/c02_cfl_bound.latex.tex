% Auto-generated by scripts/c02_cfl_bound.py
% Do not edit manually.

% Linear scalar advection CFL (1D, LF upwind)
\begin{equation}\label{eq:C2-1D-LF}
|\nu|\,\leq\, 1 \qquad \text{where } \nu = a\,\Delta t / \Delta x \qquad\text{(amplification factor }|g|^2 = 1 + (\nu^2 - 1)\sin^2(k \Delta x)\text{)}
\end{equation}

% Fastest Euler wave speed
\begin{equation}\label{eq:C2-euler-wave}
\lambda_{\max}(\mathcal{A}_x) \;=\; |u| + c, \qquad\text{where } c \;=\; \sqrt{\gamma P / \rho}
\end{equation}

% Strang-split 2D CFL (independent sweeps)
\begin{equation}\label{eq:C2-Strang-cfl}
\Delta t \cdot \max\biggl\{\frac{|u| + c}{\Delta x}\biggr\} \leq \sigma_{\mathrm{1D}}, \qquad \Delta t \cdot \max\biggl\{\frac{|v| + c}{\Delta y}\biggr\} \leq \sigma_{\mathrm{1D}}
\end{equation}

% Combined 2D-style estimator (as used in kernel)
\begin{equation}\label{eq:C2-combined}
\Delta t \cdot \max\biggl\{\frac{|u| + c}{\Delta x} + \frac{|v| + c}{\Delta y}\biggr\} \;\leq\; \sigma \qquad\text{(sigma = 0.4 in the Strang solver)}
\end{equation}

% Stability margin comparison
\begin{equation}\label{eq:C2-margin}
\text{split 1D bound: } \sigma_{\mathrm{max}} = 1.0;\qquad\text{unsplit 2D MUSCL bound: } \sigma_{\mathrm{max}} \approx 0.5;\qquad\text{kernel: } 0.4
\end{equation}
