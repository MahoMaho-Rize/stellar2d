% Auto-generated by scripts/c04_entropy_invariant.py
% Do not edit manually.

% Specific entropy
\begin{equation}\label{eq:C4-entropy}
s \;=\; \log\bigl(P\,\rho^{-\gamma}\bigr) \;=\; \log P \;-\; \gamma\,\log\rho
\end{equation}

% Smooth-flow invariant (strong form)
\begin{equation}\label{eq:C4-invariant}
\bigl(\partial_t + \mathbf{v}\!\cdot\!\nabla\bigr)\,s \;\equiv\; D_{t}s \;=\; 0 \qquad\text{(on every smooth Euler solution)}
\end{equation}

% Equivalent: entropy function conservation
\begin{equation}\label{eq:C4-K}
D_{t}\bigl(P\,\rho^{-\gamma}\bigr) \;=\; 0 \qquad\text{(equivalent to } D_{t}s = 0 \text{ via chain rule)}
\end{equation}

% Derivation path
\begin{equation}\label{eq:C4-deriv}
\text{mass: } D_{t}\rho \;=\; -\rho\,\nabla\!\cdot\!\mathbf{v},\qquad \text{energy (adiabatic): } D_{t}P \;=\; -\gamma\,P\,\nabla\!\cdot\!\mathbf{v} \;\Longrightarrow\; \tfrac{D_t P}{P} - \gamma\,\tfrac{D_t \rho}{\rho} \;=\; 0
\end{equation}

% Shock inequality (contrast with §A5)
\begin{equation}\label{eq:C4-shock}
\text{at shocks: } D_{t}s \;>\; 0 \qquad\text{(entropy inequality from §A5)}
\end{equation}
