% Auto-generated by scripts/d01_entropy_wave.py
% Do not edit manually.

% Entropy-wave IC (smooth, periodic)
\begin{equation}\label{eq:D1-IC}
\rho(x, t) = \rho_{0} + A\,\sin\bigl(k(x - u_{0} t)\bigr),\quad u = u_{0},\quad v = 0,\quad P = P_{0}
\end{equation}

% Exact Euler PDE satisfaction
\begin{equation}\label{eq:D1-PDE}
\partial_t \rho + \partial_x(\rho u) = 0,\quad \partial_t P = 0 \;\;\text{(since } P \text{ const)},\quad \partial_t E + \partial_x\bigl((E+P) u\bigr) = 0
\end{equation}

% HLLC contact-wave speed on entropy-wave pair
\begin{equation}\label{eq:D1-S-star}
S_{\star} \;=\; u_{0} \qquad\text{(pure upwind: no acoustic contribution)}
\end{equation}

% Entropy advection
\begin{equation}\label{eq:D1-entropy}
s(x, t) = \log P_{0} - \gamma \log \rho(x, t),\qquad D_{t}s = 0 \;\;\text{(along streamline)}
\end{equation}

% Periodicity at t = T = L_x / u_0
\begin{equation}\label{eq:D1-period}
\rho(x,\,T) \;=\; \rho(x,\,0) \quad \forall x \in [0, L_{x}] \qquad\text{(one full wavelength)}
\end{equation}
