% Auto-generated by scripts/d02_acoustic_linwave.py
% Do not edit manually.

% Acoustic linwave IC (right-going)
\begin{equation}\label{eq:D2-IC}
\begin{aligned}\delta\rho / \rho_0   \;&=\; \epsilon\,\sin\!\bigl(k(x - (u_0 + c_0) t)\bigr) \\\delta u   / c_0      \;&=\; \epsilon\,\sin\!\bigl(k(x - (u_0 + c_0) t)\bigr) \\\delta P   / (\gamma P_0) \;&=\; \epsilon\,\sin\!\bigl(k(x - (u_0 + c_0) t)\bigr)\end{aligned}
\end{equation}

% Adiabatic acoustic relation
\begin{equation}\label{eq:D2-adiabatic}
\delta P \;=\; c_0^{2}\,\delta\rho \qquad\text{(pressure and density perturbations in phase)}
\end{equation}

% Phase speed
\begin{equation}\label{eq:D2-phase}
v_{\mathrm{phase}} \;=\; u_0 + c_0 \qquad\text{(right-acoustic eigenvalue of }\mathcal{A}_x\text{)}
\end{equation}

% Right-eigenvector alignment
\begin{equation}\label{eq:D2-eigenvec}
\bigl(\delta\rho,\,\delta u,\,\delta v,\,\delta P\bigr)^{\mathsf T} \;\propto\; (\rho_0,\,c_0,\,0,\,\gamma P_0)^{\mathsf T} \;=\; \mathbf{R}_{u+c}^{\mathrm{primitive}}
\end{equation}

% LM-HLLC flag (use_lm_fix = false required for this test)
\begin{equation}\label{eq:D2-lm-flag}
\text{At low Mach } M = \epsilon,\;\; f_M \to M \text{ would suppress the physically-correct dispersion.}\\[3pt] \text{Use standard HLLC for acoustic convergence (see §E2).}
\end{equation}
