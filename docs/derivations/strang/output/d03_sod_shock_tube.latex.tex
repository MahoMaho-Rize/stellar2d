% Auto-generated by scripts/d03_sod_shock_tube.py
% Do not edit manually.

% Sod IC
\begin{equation}\label{eq:D3-IC}
\mathbf{W}_{L} \;=\; (1.0,\;0,\;1.0),\qquad \mathbf{W}_{R} \;=\; (0.125,\;0,\;0.1),\qquad \gamma = 1.4
\end{equation}

% Riemann fan structure
\begin{equation}\label{eq:D3-fan}
\text{L state} \;\to\; \text{rarefaction} \;\to\; \text{left star} \;\to\; \text{contact} \;\to\; \text{right star} \;\to\; \text{shock} \;\to\; \text{R state}
\end{equation}

% Star-region algebraic equation (Toro §4)
\begin{equation}\label{eq:D3-f}
u_{L} - u_{R} \;=\; f_{L}(p_{*};\,\rho_{L},\,P_{L}) \;+\; f_{R}(p_{*};\,\rho_{R},\,P_{R})
\end{equation}

% Rarefaction f-function
\begin{equation}\label{eq:D3-f-rar}
f_{\mathrm{rar}}(p;\,c_{K},\,P_{K}) \;=\; \frac{2 c_{K}}{\gamma - 1}\,\bigl[(p / P_{K})^{(\gamma-1)/(2\gamma)} - 1\bigr]
\end{equation}

% Shock f-function
\begin{equation}\label{eq:D3-f-shock}
f_{\mathrm{shock}}(p;\,\rho_{K},\,P_{K}) \;=\; (p - P_{K})\,\sqrt{\frac{A_{K}}{p + B_{K}}},\qquad A_{K} = \tfrac{2}{\rho_{K}(\gamma+1)},\;\; B_{K} = \tfrac{\gamma-1}{\gamma+1} P_{K}
\end{equation}

% Contact invariants
\begin{equation}\label{eq:D3-contact}
p_{*L} \;=\; p_{*R} \;=\; p_{*},\qquad u_{*L} \;=\; u_{*R} \;=\; u_{*} \qquad\text{(by §A8 strong-form)}
\end{equation}
