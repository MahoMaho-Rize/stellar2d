% Auto-generated by scripts/d05_bubble_init.py
% Do not edit manually.

% Bubble IC (entropy boost + azimuthal mode)
\begin{equation}\label{eq:D5-IC}
s(\mathbf{x}) \;=\; s_{\mathrm{bg}} \;+\; \delta s\,\exp\!\bigl(-r^{2}/R_{0}^{2}\bigr)\,\bigl(1 + \epsilon\,\cos(k_{\theta}\,\theta)\bigr)
\end{equation}

% Constant-pressure assumption
\begin{equation}\label{eq:D5-const-P}
P(\mathbf{x}) \;=\; \bar p(y) \qquad \text{(pressure equilibrium with HSE)}
\end{equation}

% Density from isentropic closure
\begin{equation}\label{eq:D5-rho}
\rho(\mathbf{x}) \;=\; \bar\rho(y)\,\exp\!\bigl(-\delta s_{\mathrm{local}} / \gamma\bigr)\qquad\text{(hot bubble = density deficit)}
\end{equation}

% Linear-order density perturbation
\begin{equation}\label{eq:D5-linear}
\frac{\delta\rho}{\bar\rho} \;\approx\; -\frac{\delta s_{\mathrm{local}}}{\gamma} \qquad\text{(for small } \delta s\text{)}
\end{equation}

% Zero energy perturbation (static)
\begin{equation}\label{eq:D5-dE-zero}
\delta E \;=\; (P - \bar p)/(\gamma-1) + \tfrac{1}{2}\rho(u^2 + v^2) \;=\; 0 \qquad\text{(since } P = \bar p \text{ and } u = v = 0\text{)}
\end{equation}
