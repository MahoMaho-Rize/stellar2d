% Auto-generated by scripts/d06_hse_zero_perturbation_lock.py
% Do not edit manually.

% HSE zero-perturbation IC
\begin{equation}\label{eq:D6-IC}
(\delta\rho, m_x, m_y, \delta E)(\mathbf{x}, 0) \;=\; (0, 0, 0, 0) \quad \forall\,\mathbf{x}
\end{equation}

% Flux-divergence balance on HSE
\begin{equation}\label{eq:D6-balance}
\underbrace{-\frac{\Delta t}{\Delta y}\,[\bar p(y_{j+1/2}) - \bar p(y_{j-1/2})]}_{\text{y-mom flux div}} \;+\; \underbrace{-\Delta t\,\bar\rho_{j}\,g}_{\text{gravity source}} \;=\; 0 \qquad\text{(to O(}\Delta y^{2}\text{) by §B3)}
\end{equation}

% Round-off drift bound
\begin{equation}\label{eq:D6-drift}
\|\mathbf{U}_{\mathrm{store}}\|_{\infty} \;\leq\; \epsilon_{\mathrm{mach}}\,N_{\mathrm{step}} \qquad \text{(linear accumulation; } \sqrt{N} \text{ with Kahan)}
\end{equation}
