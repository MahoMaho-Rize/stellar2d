% Auto-generated by scripts/e02_linwave_lm_hllc_order.py
% Do not edit manually.

% Acoustic dispersion relation (linear MUSCL-HLLC)
\begin{equation}\label{eq:E2-dispersion}
\omega(k) \;=\; c\,k \;-\; i\,\nu_{\mathrm{eff}}\,k^{2} \;+\; O(k^{3})
\end{equation}

% Effective numerical viscosity
\begin{equation}\label{eq:E2-nu-eff}
\nu_{\mathrm{eff}} \;=\; f_{M}\,\frac{c\,\Delta x}{2}
\end{equation}

% Per-period amplitude decay
\begin{equation}\label{eq:E2-decay}
\frac{A(T)}{A(0)} \;=\; \exp\!\bigl(-\nu_{\mathrm{eff}}\,k^{2}\,T\bigr) \;=\; \exp\!\bigl(-f_{M}\,c\,k^{2}\,\Delta x\,T/2\bigr)
\end{equation}

% Standard HLLC convergence rate
\begin{equation}\label{eq:E2-standard-rate}
f_{M} = 1 \;\Longrightarrow\; p \;=\; 2.0 \qquad\text{(pressure dissipation dominates)}
\end{equation}

% LM-HLLC sub-optimal convergence (artifact)
\begin{equation}\label{eq:E2-LM-rate}
f_{M} = M_{\mathrm{cut}} \;\;\text{at low M}\;\Longrightarrow\; p > 2 \qquad\text{(spurious super-convergence; see §C3 dispersion ratio)}
\end{equation}
