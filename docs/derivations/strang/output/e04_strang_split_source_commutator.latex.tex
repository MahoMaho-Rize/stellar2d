% Auto-generated by scripts/e04_strang_split_source_commutator.py
% Do not edit manually.

% Correct gravity absorption (inside Y)
\begin{equation}\label{eq:E4-correct}
L_{\mathrm{correct}}(\Delta t) \;=\; e^{(\Delta t/2)\mathcal{X}}\,e^{\Delta t\,(\mathcal{Y}_{\mathrm{hyd}} + \mathcal{Y}_{\mathrm{grav}})}\,e^{(\Delta t/2)\mathcal{X}}
\end{equation}

% Strang 2nd-order preserved (by §A13)
\begin{equation}\label{eq:E4-2nd-order}
L_{\mathrm{correct}}(\Delta t) \;-\; e^{\Delta t\,(\mathcal{X} + \mathcal{Y}_{\mathrm{tot}})} \;=\; O(\Delta t^{3})
\end{equation}

% Wrong (separate Z operator): 1st-order Lie-type
\begin{equation}\label{eq:E4-wrong}
L_{\mathrm{wrong}}(\Delta t) \;=\; e^{\Delta t\,\mathcal{Z}_{\mathrm{grav}}}\,L_{\mathrm{strang}}(\Delta t) \;-\; e^{\Delta t\,(\mathcal{X} + \mathcal{Y}_{\mathrm{hyd}} + \mathcal{Z}_{\mathrm{grav}})} \;=\; \tfrac{\Delta t^{2}}{2}\,[\mathcal{Z}_{\mathrm{grav}},\,\mathcal{X} + \mathcal{Y}_{\mathrm{hyd}}] + O(\Delta t^{3})
\end{equation}
