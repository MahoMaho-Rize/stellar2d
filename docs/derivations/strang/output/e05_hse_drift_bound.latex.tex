% Auto-generated by scripts/e05_hse_drift_bound.py
% Do not edit manually.

% HSE drift bound (linear summation)
\begin{equation}\label{eq:E5-bound}
\max_{t}\,\|\boldsymbol{\delta U}\|_{\infty}(N_{\mathrm{step}}) \;\leq\; \varepsilon_{\mathrm{mach}}\,N_{\mathrm{step}}\,\kappa(\mathrm{HSE})
\end{equation}

% Condition number
\begin{equation}\label{eq:E5-kappa}
\kappa(\mathrm{HSE}) \;\sim\; \frac{\max_{y}\bar\rho(y)}{\min_{y}\bar\rho(y)} \;\sim\; \biggl(\frac{1}{1 - L_{y}/y^{\star}}\biggr)^{1/(\gamma - 1)}
\end{equation}

% Canonical value
\begin{equation}\label{eq:E5-canonical}
\kappa \;\approx\; 2.3,\quad \text{at } L_{y}/y^{\star} = 0.286,\; \gamma = 1.4
\end{equation}

% Kahan summation (not in kernel)
\begin{equation}\label{eq:E5-kahan}
\text{if kernel used Kahan: } \|\boldsymbol{\delta U}\|_{\infty} \;\lesssim\; \varepsilon_{\mathrm{mach}}\,\sqrt{N_{\mathrm{step}}}\,\kappa
\end{equation}
