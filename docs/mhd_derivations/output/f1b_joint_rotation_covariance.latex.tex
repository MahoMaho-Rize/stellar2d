% Auto-generated by scripts/f1b_joint_rotation_covariance.py
% Do not edit manually.

% Joint rotation covariance (strong form)
\begin{equation}\label{eq:F1b_covariance}
\text{spec}\bigl[A_W(\mathbf{B}, \hat{\mathbf{k}}=R(\theta)\hat{\mathbf{x}})\bigr] = \text{spec}\bigl[A_W(R^{-1}(\theta)\mathbf{B}, \hat{\mathbf{x}})\bigr]
\end{equation}

% B·k̂ invariant under joint rotation
\begin{equation}\label{eq:F1b_Bdotk}
(R^{-1}(\theta)\mathbf{B})\cdot\hat{\mathbf{x}} = \mathbf{B}\cdot R(\theta)\hat{\mathbf{x}} = \mathbf{B}\cdot\hat{\mathbf{k}}
\end{equation}

% All 7 wave speeds are joint-rotation invariant
\begin{equation}\label{eq:F1b_spectrum}
\{v_0, v_0 \pm c_{Ax}, v_0 \pm c_s, v_0 \pm c_f\}\ \text{invariant}
\end{equation}

% F1 status retroactively strengthened
\begin{equation}\label{eq:F1b_retrofit}
F1's weak form (\text{spectrum depends only on }|B|, B\cdot\hat{k}, \rho, p)\text{ → F1b strong form (joint rotation gives same spectrum)}
\end{equation}
